# cross-platform
pypresence>=4.6.1
requests>=2.28.0
Pillow>=10.0.0

# windows: window scraping, tray, Credential Manager, SMTC media session
psutil>=5.9.0;                              sys_platform == "win32"
pystray>=0.19.0;                            sys_platform == "win32"
pywin32>=306;                               sys_platform == "win32"
winrt-Windows.Media.Control>=2.0;           sys_platform == "win32"
winrt-Windows.Foundation>=2.0;              sys_platform == "win32"
winrt-Windows.Foundation.Collections>=2.0;  sys_platform == "win32"

# windows: glass desktop UI (pywebview over the system WebView2 runtime)
pywebview>=5.0;                             sys_platform == "win32"
pythonnet>=3.0;                             sys_platform == "win32"

# linux: MPRIS media session over D-Bus
jeepney>=0.8;                               sys_platform == "linux"
